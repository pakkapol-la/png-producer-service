export interface LocationInfo {
    atm_no: string;
    branch_name: string;
    address: string;
    province: string;
    postcode: string;
    phone: string;
    location: number[];
};

export default LocationInfo;
